# Powerlevel10k — utilise le clone local, config existante réutilisée
source ~/.zsh-new/themes/powerlevel10k/powerlevel10k.zsh-theme

# Charge ta config p10k existante (pas besoin de relancer p10k configure)
[[ -f ~/.p10k.zsh ]] && source ~/.p10k.zsh
